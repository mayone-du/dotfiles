name: Brew install check

on: [push]

jobs:
  build:
    runs-on: macos
    steps:
      - name: Sample Test
        run: echo "sample ci"
      # - name: Install Brew
      #   run: bin/install_brew.sh
